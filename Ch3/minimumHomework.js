/*
 funtion 'min' to take two arguments and return minimum
 Will O. 1.16.21
 */
// declare 'min' and provide for 2 parameters (integers)
function min(x, y){
  // initial if statement to compare arguments
  if (x < y){
    // print value if smaller
    console.log(x);
    // backup comparison
  } else if (y < x){
    // print value if smaller
    console.log(y);
  }
};
min(0, -10);
