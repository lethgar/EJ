/*
  Write  function countBs that takes string as
  its only argument and returns number that indicates
  how many uppercase “B” characters in string.
  Will 1.18.21
    */
function countBs(str) {
  let x,
   y = 'B';
  for (i = 0; i < str.length; i++) {
    if (str[x] = y) {
    x++;
  } else {
  console.log();
    }
  }
}
countBs('BBC');
